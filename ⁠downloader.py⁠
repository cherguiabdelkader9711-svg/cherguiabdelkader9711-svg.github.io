import os
from yt_dlp import YoutubeDL

def download_youtube(url, mode='audio'):
    download_dir = os.path.join(os.getcwd(), 'downloads')
    os.makedirs(download_dir, exist_ok=True)

    ydl_opts = {}

    if mode == 'video':
        print("🎬 جاري تحميل الفيديو بأعلى جودة متاحة...")
        ydl_opts = {
            'format': 'bestvideo+bestaudio/best',
            'outtmpl': os.path.join(download_dir, '%(title)s.%(ext)s'),
        }
    elif mode == 'audio':
        print("🎵 جاري استخراج وتحميل الملف الصوتي (MP3)...")
        ydl_opts = {
            'format': 'bestaudio/best',
            'postprocessors': [{
                'key': 'FFmpegExtractAudio',
                'preferredcodec': 'mp3',
                'preferredquality': '192',
            }],
            'outtmpl': os.path.join(download_dir, '%(title)s.%(ext)s'),
        }
    elif mode == 'text':
        print("📄 جاري استخراج ملفات النصوص والترجمة...")
        ydl_opts = {
            'skip_download': True,
            'writesubtitles': True,
            'writeautomaticsub': True,
            'subtitleslangs': ['ar', 'en'],
            'outtmpl': os.path.join(download_dir, '%(title)s_transcript.%(ext)s'),
        }

    try:
        with YoutubeDL(ydl_opts) as ydl:
            ydl.download([url])
            print("✅ تمت العملية بنجاح! تم حفظ الملفات داخل مجلد 'downloads'.")
    except Exception as e:
        print(f"❌ حدث خطأ أثناء عملية التحميل: {e}")

if __name__ == "__main__":
    print("=== نظام التحميل التلقائي (yt-dlp) ===")
    
    # جلب الرابط من متغيرات البيئة في غيت هاب أو تعيين قيمة افتراضية
    video_url = os.environ.get('YOUTUBE_URL', '').strip()
    
    if not video_url:
        video_url = input("أدخل رابط فيديو يوتيوب أو Short: ").strip()
    
    if video_url:
        download_youtube(video_url, mode='audio')
    else:
        print("⚠️ الرابط فارغ أو غير صالح!")
