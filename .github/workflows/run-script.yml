name: YouTube Video Downloader & Builder

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'رابط فيديو اليوتيوب المراد تحميله'
        required: true
        type: string

jobs:
  download-video:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yt-dlp

      - name: Run Python Downloader Script
        env:
          YOUTUBE_URL: '${{ github.event.inputs.video_url }}'
        run: |
          cat << 'EOF' > downloader.py
          import os
          from yt_dlp import YoutubeDL

          url = os.getenv('YOUTUBE_URL')
          if not url:
              print("Error: YOUTUBE_URL is not set")
              exit(1)

          print(f"Starting download for: {url}")
          ydl_opts = {
              'format': 'best',
              'outtmpl': 'downloads/%(title)s.%(ext)s',
          }

          try:
              os.makedirs('downloads', exist_ok=True)
              with YoutubeDL(ydl_opts) as ydl:
                  info = ydl.extract_info(url, download=True)
                  print(f"Successfully downloaded: {info.get('title')}")
          except Exception as e:
              print(f"Error: Could not download video - {str(e)}")
              exit(1)
          EOF

          python downloader.py

      - name: Upload Downloaded Video Artifact
        uses: actions/upload-artifact@v4
        with:
          name: youtube-downloaded-video
          path: downloads/
